// app/(hod)/layout.tsx — HOD Role Layout Placeholder
// TODO: Implement HOD dashboard layout with sidebar navigation

import type { ReactNode } from 'react';

export default function HodLayout({ children }: { children: ReactNode }) {
  return (
    <div>
      {/* TODO: Add HOD sidebar and header */}
      <main>{children}</main>
    </div>
  );
}
