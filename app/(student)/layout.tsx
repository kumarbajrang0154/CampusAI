// app/(student)/layout.tsx — Student Role Layout Placeholder
// TODO: Implement student dashboard layout with sidebar navigation

import type { ReactNode } from 'react';

export default function StudentLayout({ children }: { children: ReactNode }) {
  return (
    <div>
      {/* TODO: Add student sidebar and header */}
      <main>{children}</main>
    </div>
  );
}
