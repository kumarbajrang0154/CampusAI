// app/(admin)/layout.tsx — Admin Role Layout Placeholder
// TODO: Implement admin dashboard layout with full-width sidebar navigation

import type { ReactNode } from 'react';

export default function AdminLayout({ children }: { children: ReactNode }) {
  return (
    <div>
      {/* TODO: Add admin sidebar and header */}
      <main>{children}</main>
    </div>
  );
}
