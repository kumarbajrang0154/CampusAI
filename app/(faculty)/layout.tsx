// app/(faculty)/layout.tsx — Faculty Role Layout Placeholder
// TODO: Implement faculty dashboard layout with sidebar navigation

import type { ReactNode } from 'react';

export default function FacultyLayout({ children }: { children: ReactNode }) {
  return (
    <div>
      {/* TODO: Add faculty sidebar and header */}
      <main>{children}</main>
    </div>
  );
}
