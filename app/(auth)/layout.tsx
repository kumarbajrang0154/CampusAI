// app/(auth)/layout.tsx — Auth Layout Placeholder
// TODO: Implement auth layout with centered card, logo, and branding

import type { ReactNode } from 'react';

export default function AuthLayout({ children }: { children: ReactNode }) {
  return (
    <div>
      {/* TODO: Add auth layout with centered form container and branding */}
      <main>{children}</main>
    </div>
  );
}
